package com.uzm.core.controllers;

import com.uzm.core.Core;
import com.uzm.core.DiscordBot;
import net.dv8tion.jda.api.entities.Member;
import net.dv8tion.jda.api.entities.VoiceChannel;

public class MusicController {
  /* if (video != null) {
    Core.getPlayer().loadTrack("https://youtube.com/watch?v=" + video.getCode(), e.getMember(), e.getChannel(), false);
  }
        bot.destroyCache();
        bot.getResultYoutube().clear();
  public boolean addMusic(DiscordBot bot, String videoId, Member author, VoiceChannel voiceState) {
     try {
       Core.getPlayer().get
       bot.getResultYoutube().clear();
       bot.destroyCache(); //TODO Fix later this purger

       return true;
     }catch (Exception err) {
       return false;
     }
  }
  public boolean addMusic(String videoId, Member author, VoiceChannel voiceState) {

  }*/

}
